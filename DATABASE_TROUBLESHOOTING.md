# 🔧 Решение проблем подключения к базе данных

## Проблема: "Attempting to connect to the database..."

Если вы видите это сообщение и подключение не устанавливается, следуйте этим шагам:

## ✅ Быстрая проверка

### 1. Проверьте переменную DATABASE_URL в Railway

1. Откройте Railway Dashboard
2. Выберите ваш проект
3. Перейдите в **Variables**
4. Убедитесь, что `DATABASE_URL` установлен автоматически (при добавлении PostgreSQL)

**Если DATABASE_URL отсутствует:**
- Убедитесь, что PostgreSQL сервис добавлен в проект
- Railway должен автоматически создать переменную `DATABASE_URL`
- Если нет, добавьте PostgreSQL заново: **New** → **Database** → **Add PostgreSQL**

### 2. Проверьте статус PostgreSQL сервиса

1. В Railway Dashboard найдите сервис PostgreSQL
2. Убедитесь, что он **Running** (зеленый статус)
3. Если сервис остановлен, нажмите **Deploy** или **Restart**

### 3. Проверьте логи приложения

1. В Railway Dashboard откройте ваш backend сервис
2. Перейдите в **Deploy Logs**
3. Ищите сообщения:
   - `✓ Database connection successful!` - подключение успешно
   - `✗ Database connection failed:` - ошибка подключения

## 🔍 Типичные ошибки и решения

### Ошибка: "connection refused" или "could not connect"

**Причина:** PostgreSQL сервис не запущен или недоступен

**Решение:**
1. Проверьте статус PostgreSQL в Railway
2. Перезапустите PostgreSQL сервис
3. Убедитесь, что оба сервиса (backend и PostgreSQL) в одном проекте

### Ошибка: "authentication failed"

**Причина:** Неправильные учетные данные

**Решение:**
1. Railway автоматически устанавливает правильные учетные данные
2. Не изменяйте `DATABASE_URL` вручную
3. Если изменили, удалите переменную и Railway создаст её заново

### Ошибка: "database does not exist"

**Причина:** База данных не создана

**Решение:**
1. Railway создает базу данных автоматически
2. Проверьте, что PostgreSQL сервис полностью развернут
3. Перезапустите backend сервис после создания PostgreSQL

### Ошибка: "SSL connection required"

**Причина:** Проблемы с SSL соединением

**Решение:**
1. Код автоматически обрабатывает SSL
2. Если проблема сохраняется, проверьте формат `DATABASE_URL`
3. Убедитесь, что используется `postgresql://` (не `postgres://`)

## 🛠️ Пошаговое решение

### Шаг 1: Проверка переменных окружения

```bash
# В Railway CLI (если установлен)
railway variables
```

Должна быть переменная:
```
DATABASE_URL=postgresql://postgres:password@host:port/railway
```

### Шаг 2: Перезапуск сервисов

1. В Railway Dashboard:
   - Остановите backend сервис
   - Перезапустите PostgreSQL сервис
   - Запустите backend сервис

### Шаг 3: Проверка подключения

Откройте в браузере:
```
https://your-app.railway.app/health/db
```

Должен вернуться JSON:
```json
{
  "status": "healthy",
  "database": "connected",
  "timestamp": "2025-01-01T12:00:00"
}
```

## 📊 Проверка через API

### Health Check Endpoints:

1. **Общий статус:**
   ```
   GET https://vendhub-database-production-aab3.up.railway.app/health
   ```

2. **Статус базы данных:**
   ```
   GET https://vendhub-database-production-aab3.up.railway.app/health/db
   ```

### Использование curl:

```bash
# Проверка API
curl https://vendhub-database-production-aab3.up.railway.app/health

# Проверка БД
curl https://vendhub-database-production-aab3.up.railway.app/health/db
```

## 🔄 Пересоздание подключения

Если ничего не помогает:

1. **Удалите PostgreSQL сервис:**
   - Railway Dashboard → PostgreSQL → Settings → Delete

2. **Создайте заново:**
   - New → Database → Add PostgreSQL

3. **Railway автоматически:**
   - Создаст новую базу данных
   - Установит `DATABASE_URL`
   - Подключит к backend

4. **Перезапустите backend:**
   - Railway автоматически перезапустит при изменении переменных

## 📝 Логи для диагностики

При запуске приложение выводит:

```
Attempting to connect to the database...
Database URL: host:port/database
✓ Database connection successful!
Creating database tables...
✓ Database tables created/verified
✓ Admin user 'jamshiddin' created successfully
```

Если видите ошибки, они будут содержать детали проблемы.

## ⚠️ Важные замечания

1. **Не изменяйте DATABASE_URL вручную** - Railway управляет этим автоматически
2. **Оба сервиса должны быть в одном проекте** - иначе они не смогут подключиться
3. **Подождите 1-2 минуты** после создания PostgreSQL - база данных инициализируется
4. **Проверьте логи** - они содержат детальную информацию об ошибках

## 🆘 Если проблема не решена

1. Проверьте логи в Railway Dashboard
2. Убедитесь, что используете последнюю версию кода
3. Проверьте документацию Railway: https://docs.railway.app/databases/postgresql
4. Создайте issue в GitHub с логами ошибок

---

**Последнее обновление:** 2025-01-01

