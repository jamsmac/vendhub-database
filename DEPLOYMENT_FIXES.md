# 🔧 Исправления проблем деплоя

Этот документ описывает все исправления, внесенные для решения проблем деплоя проекта VendHub Database.

## ✅ Исправленные проблемы

### 1. **Dockerfile - обработка PORT**
**Проблема:** Неправильная обработка переменной PORT в Dockerfile
**Исправление:**
- Добавлена переменная окружения `ENV PORT=8000`
- Улучшена команда запуска для правильной обработки `${PORT:-8000}`
- Добавлены права на директорию logs

**Файл:** `Dockerfile`

### 2. **Логирование - создание директории**
**Проблема:** Директория `logs/` могла не существовать в production, что вызывало ошибки
**Исправление:**
- Добавлено автоматическое создание директории `logs/` при запуске
- Использование `os.makedirs(log_dir, exist_ok=True)` для безопасного создания
- Удален дублирующий импорт `os`

**Файл:** `backend/main.py`

### 3. **DATABASE_URL - поддержка Railway**
**Проблема:** Railway может предоставлять `postgres://` вместо `postgresql://`
**Исправление:**
- Добавлена автоматическая конвертация `postgres://` → `postgresql://`
- Улучшена загрузка `.env` файла (только если существует, для локальной разработки)
- Удалены неиспользуемые импорты

**Файл:** `backend/database.py`

### 4. **CORS конфигурация**
**Проблема:** Неправильная обработка пустых или некорректных значений ALLOWED_ORIGINS
**Исправление:**
- Добавлена проверка на пустые значения
- Правильная обработка значения `*` для всех источников
- Тримминг пробелов в списке origins

**Файл:** `backend/main.py`

### 5. **Railway конфигурация**
**Проблема:** Отсутствие правильной команды запуска для Railway
**Исправление:**
- Добавлен `startCommand` в `railway.toml`
- Команда использует переменную `$PORT` из окружения Railway

**Файл:** `railway.toml`

### 6. **Docker Compose для локальной разработки**
**Проблема:** Docker Compose ссылался на несуществующий Dockerfile в backend/
**Исправление:**
- Создан отдельный `backend/Dockerfile` для локальной разработки
- Обновлен `docker-compose.yml` с правильными переменными окружения
- Удалена команда из docker-compose (используется CMD из Dockerfile)

**Файлы:** `backend/Dockerfile`, `docker-compose.yml`

## 📋 Переменные окружения для Railway

Убедитесь, что в Railway установлены следующие переменные:

```env
JWT_SECRET_KEY=ваш-секретный-ключ-минимум-32-символа
ALLOWED_ORIGINS=*
PORT=8000
DEBUG=False
LOG_LEVEL=INFO
```

**Важно:** `DATABASE_URL` устанавливается автоматически при добавлении PostgreSQL в Railway.

## 🚀 Проверка деплоя

### Локальное тестирование:

```bash
# 1. Проверить Dockerfile
docker build -t vendhub-test .
docker run -p 8000:8000 -e DATABASE_URL=postgresql://... vendhub-test

# 2. Проверить docker-compose
docker-compose up -d
docker-compose logs -f backend
```

### Railway деплой:

1. Убедитесь, что `railway.toml` находится в корне проекта
2. Убедитесь, что `Dockerfile` находится в корне проекта
3. Добавьте PostgreSQL в Railway проект
4. Установите переменные окружения
5. Railway автоматически соберет и развернет приложение

## 🔍 Дополнительные улучшения

1. **Безопасность:** Убедитесь, что `JWT_SECRET_KEY` установлен в production
2. **CORS:** Обновите `ALLOWED_ORIGINS` с реальными доменами frontend
3. **Логи:** Логи сохраняются в `backend/logs/` и ротируются ежедневно
4. **База данных:** Railway автоматически создает и подключает PostgreSQL

## 📝 Примечания

- Все изменения обратно совместимы с локальной разработкой
- Docker Compose продолжает работать для локального тестирования
- Railway использует корневой Dockerfile для production деплоя
- Локальная разработка использует `backend/Dockerfile` через docker-compose

## ✅ Чеклист перед деплоем

- [ ] `JWT_SECRET_KEY` установлен в Railway
- [ ] `ALLOWED_ORIGINS` настроен правильно
- [ ] PostgreSQL добавлен в Railway проект
- [ ] `DATABASE_URL` установлен автоматически
- [ ] `PORT` обрабатывается правильно (Railway устанавливает автоматически)
- [ ] Логи создаются без ошибок
- [ ] CORS настроен для frontend домена

---

**Дата исправлений:** 2025-01-01
**Версия:** 1.0.0

