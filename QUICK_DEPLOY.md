# 🚀 Быстрый деплой на Railway за 5 минут

## Что вам понадобится:
- GitHub аккаунт (бесплатный)
- Railway аккаунт (бесплатный)
- 5-10 минут времени

---

## Шаг 1: Подготовка GitHub репозитория

### 1.1. Создайте новый репозиторий на GitHub
1. Перейдите на https://github.com/new
2. Назовите репозиторий: `vendhub-database`
3. Сделайте его **Private** (для безопасности)
4. НЕ добавляйте README, .gitignore или лицензию
5. Нажмите **Create repository**

### 1.2. Загрузите код в репозиторий

Откройте командную строку в папке проекта:

```bash
cd C:\Users\user\Desktop\vending\011225

# Инициализируем git (если еще не сделано)
git init

# Добавляем все файлы
git add .

# Создаем первый коммит
git commit -m "Initial commit: VendHub Database"

# Подключаем к GitHub (замените USERNAME на ваш логин GitHub)
git remote add origin https://github.com/USERNAME/vendhub-database.git

# Отправляем код
git branch -M main
git push -u origin main
```

---

## Шаг 2: Деплой на Railway

### 2.1. Регистрация на Railway
1. Перейдите на https://railway.app
2. Нажмите **Login with GitHub**
3. Авторизуйте Railway для доступа к GitHub

### 2.2. Создание проекта

1. На главной странице Railway нажмите **New Project**
2. Выберите **Deploy from GitHub repo**
3. Выберите репозиторий `vendhub-database`
4. Railway автоматически начнет деплой!

### 2.3. Добавление базы данных PostgreSQL

1. В проекте нажмите **New** → **Database** → **Add PostgreSQL**
2. Railway автоматически создаст БД и подключит к вашему приложению
3. Переменная `DATABASE_URL` будет установлена автоматически

### 2.4. Настройка переменных окружения

1. Нажмите на сервис (backend)
2. Перейдите в **Variables**
3. Добавьте следующие переменные:

```env
JWT_SECRET_KEY=вставьте-сюда-случайную-строку-минимум-32-символа
ALLOWED_ORIGINS=*
PORT=8000
DEBUG=False
LOG_LEVEL=INFO
```

**Генерация JWT_SECRET_KEY:**
```bash
# В командной строке выполните:
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 2.5. Получение URL вашего приложения

1. В разделе **Settings** найдите **Domains**
2. Нажмите **Generate Domain**
3. Railway создаст URL вида: `https://vendhub-database-production.up.railway.app`
4. **Скопируйте этот URL** - он понадобится для frontend

---

## Шаг 3: Настройка Frontend

### 3.1. Обновите API_URL в vendhub_online.html

Откройте файл `vendhub_online.html` и найдите строку:

```javascript
const API_URL = 'http://localhost:8000';
```

Замените на ваш Railway URL:

```javascript
const API_URL = 'https://vendhub-database-production.up.railway.app';
```

### 3.2. Разместите frontend

**Вариант A: Cloudflare Pages (рекомендуется, бесплатно)**

1. Перейдите на https://pages.cloudflare.com
2. Войдите через GitHub
3. Создайте новый проект: **Create a project** → **Connect to Git**
4. Выберите репозиторий `vendhub-database`
5. Настройки:
   - Framework preset: `None`
   - Build command: (оставьте пустым)
   - Build output directory: `/`
6. Нажмите **Save and Deploy**

Ваш сайт будет доступен по адресу: `https://vendhub-database.pages.dev`

**Вариант B: GitHub Pages (бесплатно)**

1. В репозитории перейдите в **Settings** → **Pages**
2. Source: `Deploy from a branch`
3. Branch: `main` / `root`
4. Нажмите **Save**

Сайт будет доступен: `https://USERNAME.github.io/vendhub-database/vendhub_online.html`

**Вариант C: Просто откройте файл локально**

Можно просто открыть `vendhub_online.html` в браузере - он будет работать с вашим Railway backend!

---

## Шаг 4: Обновление CORS (важно!)

После того как frontend развернут, обновите переменную `ALLOWED_ORIGINS`:

1. Вернитесь в Railway → Variables
2. Обновите `ALLOWED_ORIGINS`:

```
https://vendhub-database.pages.dev,https://USERNAME.github.io,file://
```

3. Railway автоматически перезапустит приложение

---

## 🎉 Готово!

Ваше приложение работает онлайн 24/7!

### Адреса:
- **Backend API:** https://vendhub-database-production.up.railway.app
- **API Docs:** https://vendhub-database-production.up.railway.app/docs
- **Frontend:** https://vendhub-database.pages.dev

---

## 📊 Мониторинг и управление

### Railway Dashboard
- **Логи:** Railway → Deploy Logs (смотрите логи в реальном времени)
- **Метрики:** Railway → Metrics (CPU, память, сеть)
- **База данных:** Railway → PostgreSQL → Data (SQL запросы)

### Стоимость
- **Free Tier:** $5 в месяц бесплатно
- **Usage-based:** $0.000463/GB-hour для RAM, $0.000231/vCPU-hour

---

## 🔧 Полезные команды

### Локальное тестирование перед деплоем

```bash
# Backend
cd backend
python main.py

# Открыть в браузере
start http://localhost:8000/docs
```

### Обновление кода на Railway

```bash
# Сделайте изменения в коде
git add .
git commit -m "Update: описание изменений"
git push

# Railway автоматически развернет новую версию!
```

### Просмотр логов локально

```bash
# В Railway CLI (установите: npm i -g @railway/cli)
railway login
railway logs
```

---

## 🆘 Решение проблем

### Ошибка: "Application failed to respond"
- Проверьте логи в Railway
- Убедитесь что `PORT` установлен в переменных
- Проверьте что `DATABASE_URL` установлен автоматически

### Ошибка: "CORS error"
- Убедитесь что frontend URL добавлен в `ALLOWED_ORIGINS`
- Добавьте `*` для разрешения всех источников (только для тестирования!)

### База данных не работает
- Убедитесь что PostgreSQL plugin добавлен
- Проверьте что `DATABASE_URL` появился в переменных автоматически
- Попробуйте перезапустить сервис

### Frontend не подключается к backend
- Проверьте `API_URL` в `vendhub_online.html`
- Убедитесь что используете `https://` (не `http://`)
- Проверьте CORS настройки

---

## 📚 Дополнительные ресурсы

- [Railway Docs](https://docs.railway.app)
- [Cloudflare Pages Docs](https://developers.cloudflare.com/pages)
- [FastAPI Docs](https://fastapi.tiangolo.com)

---

## 💡 Советы

1. **Используйте разные секретные ключи** для development и production
2. **Регулярно делайте бэкапы** базы данных (Railway → PostgreSQL → Backups)
3. **Мониторьте использование** в Railway Dashboard
4. **Обновляйте зависимости** регулярно: `pip install --upgrade -r requirements.txt`

---

Нужна помощь? Создайте issue на GitHub или напишите в поддержку Railway.

**Удачи! 🚀**
